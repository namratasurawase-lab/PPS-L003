#include <stdio.h>
int main(){
    int i,j,k;
    for (i=0 ; i<=5 ; i++){
             for (j=5-i ; j>=1 ; j--){ 
                 printf ("*\t");
             }
             for (k=1 ; k<=i ; k++){
                 printf ("*\t"); 
             }
   }
   printf ("\n"); 
   return 0;
}
    
